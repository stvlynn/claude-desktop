// Restored from ref/webview/assets/file-diff-KtBt16gK.js
// file-diff-KtBt16gK chunk restored from the Codex webview bundle.
// @ts-nocheck
// Vendored @pierre/diffs fork boundary.
//
// Codex bundles this renderer with local settings/theme integration
// (diff marker style, resolved code themes, and Codex surface CSS), so this is
// not a clean bare re-export of the published @pierre/diffs package. Keep the
// original forked runtime reachable until the renderer cluster is restored as a
// dedicated vendored subtree.
// TODO: restore the forked @pierre/diffs renderer internals.

export {
  n as parseDiffFromFiles,
  t as FileDiff,
} from "../../../ref/webview/assets/file-diff-KtBt16gK.js";
