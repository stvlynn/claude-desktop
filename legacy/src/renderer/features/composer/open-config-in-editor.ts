// Restored from ref/webview/assets/hooks-settings-DUMdgBqk.js
// Partial stub for opening config in editor to unblock hooks-settings-page promotion.

export function openConfigInEditor(_configPath?: string): void {
  // no-op
}
// Added to satisfy relative import checks.
export const initOpenConfigInEditorChunk = undefined as never;
