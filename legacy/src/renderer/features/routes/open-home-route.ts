// Restored from ref/webview/assets/open-home-route-*.js
// Partial stub for open-home-route to unblock command-menu promotion.

export function useOpenHomeRoute(): () => void {
  return () => {};
}
// Added to satisfy relative import checks.
export const initOpenHomeRouteChunk = undefined as never;
