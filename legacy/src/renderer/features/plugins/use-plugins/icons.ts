// Restored from ref/webview/assets/use-plugins-Dex8E4w_.js
// Minimal icons stub for the use-plugins cluster.

export function resolvePluginIconPaths(_pluginId: string): {
  iconPath?: string | null;
  iconMediumPath?: string | null;
} {
  return { iconPath: null, iconMediumPath: null };
}
