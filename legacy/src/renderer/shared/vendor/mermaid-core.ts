// Restored from ref/webview/assets/mermaid-core.js
// No-op compatibility shim for the mermaid diagram renderer core. Restored
// modules import through the vendor boundary; this stub avoids adding the full
// mermaid npm dep until the real diagram rendering is wired.
export function MermaidCore(): null {
  return null;
}
