// Restored from ref/webview/assets/react-intl.js
// Re-export the npm react-intl package through the vendor boundary so restored
// chunks can keep their original import paths while src/ uses the real package.
export * from "react-intl";

export function initIntlRuntime(): void {}
