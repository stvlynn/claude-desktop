// Restored from ref/webview/assets/react-router.js
// Re-export the npm react-router package through the vendor boundary so restored
// chunks can keep their original import paths while src/ uses the real package.
export * from "react-router";
