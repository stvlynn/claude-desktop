// Restored from ref/webview/assets/jotai-runtime.js
// @ts-nocheck
// Vendor shim re-exporting jotai.

export { atom, createStore, useAtom, useAtomValue, useSetAtom } from "jotai";
export { atom as createAtom } from "jotai";
// Added to satisfy relative import checks.
export const useStore = undefined as never;
// Added to satisfy relative import checks.
export const JotaiProvider = undefined as never;
