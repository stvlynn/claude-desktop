# Renderer Restore

Feature-Sliced Design target for Claude Desktop renderer code.

- `app/` application wiring.
- `pages/` route or window entry points.
- `widgets/` composed page regions.
- `features/` user-facing workflows.
- `entities/` domain objects used by features.
- `shared/` UI, i18n, API boundaries, and utilities.
